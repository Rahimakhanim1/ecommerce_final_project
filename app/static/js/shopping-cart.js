let updateCart = document.querySelectorAll('.item-quantity')
$('. updateItemValue').on('click',function(){
    data = {['id':1,'itemId':12,'value':3]}
    k=1
    updateCart.forEach(
        data.id = k
    )
})

console.log(data)